spring.application.name=prueba

spring.datasource.url=jdbc:mysql://localhost:3306/mysql?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Clave123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Crea las tablas automáticamente si no existen y actualiza el esquema
spring.jpa.hibernate.ddl-auto=update

# Dialecto para MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Mostrar SQL en consola (opcional)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
